# Commands

## Run the Flask app
cd backend; export OPENROUTER_API_KEY=sk-or-v1-... python -m flask run

## Install requirements
cd backend; pip install -r requirements.txt

# OpenRouter Setup
Set OPENROUTER_API_KEY environment variable or add to .env file in backend folder.
